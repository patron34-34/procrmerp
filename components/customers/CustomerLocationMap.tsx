import React from 'react';

// This component is no longer in use as the map has been removed from the customer detail page
// for a cleaner and more focused layout. Map functionality is available on the main customers page.
const CustomerLocationMap: React.FC = () => {
    return null;
};

export default CustomerLocationMap;